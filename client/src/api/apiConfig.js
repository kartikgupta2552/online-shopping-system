/*
    to avoid hardcoding API URLs directly in components, we define them in a central config file.
*/

const BASE_URL = "http://localhost:8080"; // Your Spring Boot backend URL

export default BASE_URL;
